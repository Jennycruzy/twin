# Catalog repair proposal: `raw_pg.orders.customer_id`

This proposal is generated from a measured catalog gap. It is intentionally a normal patch
for review; Twin does not apply it to the warehouse or open a remote pull request.

## Finding

The `commerce` graph `2b0ff33cd937f51f` contains table lineage from `raw_pg.orders`
but no column lineage for `raw_pg.orders.customer_id`. The column exists in the observed warehouse
schema and the fault scenario `customer_id_nulled_at_source` exercises it. The missing field-level context
forces Twin to use a conservative table-grain prediction.

Current context confidence for `raw_pg.orders` is **0.85 (high)**.
The proposal changes `estate/dbt/models/sources.yml` by declaring the source column explicitly, giving dbt
and its metadata ingestion a stable source contract from which column lineage can be emitted.

Direct table consumers identified in the graph:

- `staging.stg_orders`

## Proposed change

```diff
--- a/estate/dbt/models/sources.yml
+++ b/estate/dbt/models/sources.yml
@@ -42,6 +42,8 @@
       - name: orders
         description: Order headers.
         meta: {owner: priya.raghavan@example.com, team: data-platform, refresh_cadence: daily_0500, sla_hours: 6, replicated: true, criticality_tier: tier1}
+        columns:
+          - name: customer_id
       - name: order_items
         description: Order lines.
         meta: {owner: priya.raghavan@example.com, team: data-platform, refresh_cadence: daily_0500, sla_hours: 6, replicated: true, criticality_tier: tier1}
```

## Validation

1. Apply the patch and run `make estate-build TARGET=commerce`.
2. Re-ingest the dbt metadata with `make estate-ingest TARGET=commerce`, then rebuild the
   target graph with `make read TARGET=commerce`.
3. Confirm `raw_pg.orders.customer_id` has a column edge to the expected landing fields.
4. Re-run `make run TARGET=commerce SCENARIO=scenarios/customer_id_nulled_at_source.yml` and compare the scorecard with the
   recorded `customer_id_nulled_at_source` result. A better precision score is evidence; an unchanged score
   is also a valid result if the catalog ingestion does not derive field lineage from source
   declarations.
5. Run `make gate` and keep this proposal only if the graph fingerprint and scenario evidence
   explain the resulting change.

## Review notes

- The change is metadata-only: it does not alter source data or model SQL.
- The column name is copied from the warehouse schema; no semantic value is invented here.
- The proposal was generated by commit `67955bc` (dirty working tree) from graph
  `2b0ff33cd937f51f`. Regenerate it after applying the change rather than editing the
  artifact by hand.
