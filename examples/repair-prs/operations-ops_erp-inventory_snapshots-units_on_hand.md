# Catalog repair proposal: `ops_erp.inventory_snapshots.units_on_hand`

This proposal is generated from a measured catalog gap. It is intentionally a normal patch
for review; Twin does not apply it to the warehouse or open a remote pull request.

## Finding

The `operations` graph `464884b669b60aef` contains table lineage from `ops_erp.inventory_snapshots`
but no column lineage for `ops_erp.inventory_snapshots.units_on_hand`. The column exists in the observed warehouse
schema and the fault scenario `inventory_units_nulled_at_source` exercises it. The missing field-level context
forces Twin to use a conservative table-grain prediction.

Current context confidence for `ops_erp.inventory_snapshots` is **0.78 (partial)**.
The proposal changes `operations_estate/dbt/models/sources.yml` by declaring the source column explicitly, giving dbt
and its metadata ingestion a stable source contract from which column lineage can be emitted.

Direct table consumers identified in the graph:

- `ops_staging.stg_inventory_snapshots`

## Proposed change

```diff
--- a/operations_estate/dbt/models/sources.yml
+++ b/operations_estate/dbt/models/sources.yml
@@ -32,6 +32,8 @@
           - name: priority
       - name: inventory_snapshots
         meta: {owner: lin.nguyen@example.com, team: network-operations, refresh_cadence: every_3_days, sla_hours: 12, replicated: false, criticality_tier: tier2}
+        columns:
+          - name: units_on_hand
       - name: work_orders
         meta: {owner: diego.mora@example.com, team: facilities, refresh_cadence: hourly, sla_hours: 4, replicated: true, criticality_tier: tier2}
```

## Validation

1. Apply the patch and run `make estate-build TARGET=operations`.
2. Re-ingest the dbt metadata with `make estate-ingest TARGET=operations`, then rebuild the
   target graph with `make read TARGET=operations`.
3. Confirm `ops_erp.inventory_snapshots.units_on_hand` has a column edge to the expected landing fields.
4. Re-run `make run TARGET=operations SCENARIO=operations_scenarios/inventory_units_nulled_at_source.yml` and compare the scorecard with the
   recorded `inventory_units_nulled_at_source` result. A better precision score is evidence; an unchanged score
   is also a valid result if the catalog ingestion does not derive field lineage from source
   declarations.
5. Run `make gate` and keep this proposal only if the graph fingerprint and scenario evidence
   explain the resulting change.

## Review notes

- The change is metadata-only: it does not alter source data or model SQL.
- The column name is copied from the warehouse schema; no semantic value is invented here.
- The proposal was generated by commit `67955bc` (dirty working tree) from graph
  `464884b669b60aef`. Regenerate it after applying the change rather than editing the
  artifact by hand.
